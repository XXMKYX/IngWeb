<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('elements/header') %>
</head>

<body>

    <header class="site-header inicio">
        <%- include('elements/banner') %>
    </header>

    <main class="contenedor seccion">
        <!-- MENU -->
        <%- include('elements/menu-global') %>
            <!-- FORMULARIO PERSONAL -->
            <div class="grid centrar-columnas forma">


    </main>
</body>

<footer>
    <p>HUEBOS</p>
    <% preg.forEach(function(pre){ %>
        <div class="datos">
            <p>x2</p>
            <p>
                <%= pre.nombre %>
            </p>
            <p>
                <%= pre.curp %>
            </p>
        </div>
        <% }) %>
</footer>

</html>